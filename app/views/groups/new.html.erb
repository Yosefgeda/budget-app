<div class="nav-bar">
     <a href="<%= groups_path %>">
            <i class="fa-solid fa-arrow-left"></i>
        </a>
    <h2>New Group</h2>
    <a href="<%= destroy_user_session_path %>">
        <i class="fa-solid fa-right-from-bracket"></i>
    </a>
</div>
<%= form_with(model: @group)do |form| %>
    <div class="form-container">
        <%= form.text_field :name, placeholder: "Name" , class: "new-group-form" %>
        <%= form.text_field :icon, placeholder: "Icon Link", class: "new-group-form" %>
        <%= form.submit "CREATE", class: "submit-button-group" %>
    </div>
<% end %>